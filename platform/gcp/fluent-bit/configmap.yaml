apiVersion: v1
kind: ConfigMap
metadata:
  name: fluent-bit-config
  labels:
    app: fluent-bit
    area: logging
data:
  fluent-bit.conf: |
    [SERVICE]
        Flush         1
        Log_Level     info
        Parsers_File  parsers.conf
        HTTP_Server   On
        HTTP_Listen   0.0.0.0
        HTTP_Port     2020

    # 모든 노드의 CRI 컨테이너 로그를 읽는다. DaemonSet이므로 노드당 1개만 실행된다.
    [INPUT]
        Name              tail
        Path              /var/log/containers/*.log
        Tag               kube.*
        Parser            cri
        DB                /fluent-bit/state/flb_kube.db
        Mem_Buf_Limit     10MB
        Skip_Long_Lines   On
        Refresh_Interval  10

    # Pod/Namespace 메타데이터(라벨, namespace, pod, container)를 붙인다.
    [FILTER]
        Name                kubernetes
        Match               kube.*
        Kube_Tag_Prefix     kube.var.log.containers.
        Labels              On
        Annotations         Off
        Namespace_Labels    On
        Merge_Log           On
        Keep_Log            On
        K8S-Logging.Parser  On
        K8S-Logging.Exclude On

    # 어느 노드에서 수집했는지 보존한다. platform 라벨은 앱 Pod 라벨에 있으면
    # kubernetes.labels.platform으로 함께 들어가며, 없는 워크로드는 node 기준으로 구분한다.
    [FILTER]
        Name    record_modifier
        Match   kube.*
        Record  collector_node ${NODE_NAME}

    # Keep only the operational namespaces in the platform index. Audio app
    # JSON logs will be added later as a separate, longer-retained index.
    [FILTER]
        Name   grep
        Match  kube.*
        Regex  $kubernetes['namespace_name'] ^(logging|devops|istio-system)$

    # Dashboard query fields are flattened here while the original `log` and
    # Kubernetes metadata are retained for incident investigation.
    [FILTER]
        Name    lua
        Match   kube.*
        Script  /fluent-bit/etc/platform-normalize.lua
        Call    normalize

    [OUTPUT]
        Name                opensearch
        Match               kube.*
        Host                opensearch-cluster-master.logging.svc.cluster.local
        Port                9200
        Index               cantaloupe-platform-logs
        Suppress_Type_Name  On
        Generate_ID         On
        Retry_Limit         False
        tls                 Off
  parsers.conf: |
    [PARSER]
        Name         cri
        Format       regex
        Regex        ^(?<time>[^ ]+) (?<stream>stdout|stderr) (?<logtag>[^ ]+) (?<log>.*)$
        Time_Key     time
        Time_Format  %Y-%m-%dT%H:%M:%S.%L%z
  platform-normalize.lua: |
    -- Common schema for platform log dashboards. `collector_platform` is
    -- read once from the node label captured by the init container.
    local collector_platform = nil

    local function get_collector_platform()
      if collector_platform ~= nil then
        return collector_platform
      end

      local file = io.open("/fluent-bit/state/collector_platform", "r")
      if file == nil then
        collector_platform = "unknown"
        return collector_platform
      end

      collector_platform = file:read("*l") or "unknown"
      file:close()
      return collector_platform
    end

    function normalize(tag, timestamp, record)
      local kubernetes = record["kubernetes"] or {}
      local pod_labels = kubernetes["labels"] or {}
      local namespace_metadata = record["kubernetes_namespace"] or {}
      local namespace_labels = namespace_metadata["labels"] or {}

      record["namespace"] = kubernetes["namespace_name"] or "unknown"
      record["area"] = pod_labels["area"] or namespace_labels["area"] or "unknown"
      record["app"] = pod_labels["app"] or pod_labels["app.kubernetes.io/name"] or kubernetes["container_name"] or "unknown"
      record["collector_platform"] = get_collector_platform()
      record["pod"] = kubernetes["pod_name"] or "unknown"
      record["container"] = kubernetes["container_name"] or "unknown"
      record["message"] = record["msg"] or record["message"] or record["log"] or ""

      if record["level"] == nil then
        record["level"] = "unknown"
      end

      return 1, timestamp, record
    end
