apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: harbor-vs
  namespace: devops
spec:
  hosts:
  - "*"
  gateways:
  - istio-ingress/istio-gateway
  http:
  - match:
    - uri:
        prefix: /harbor
    route:
    - destination:
        host: harbor-portal   # UI 타겟 서비스 이름 수정
        port:
          number: 80
