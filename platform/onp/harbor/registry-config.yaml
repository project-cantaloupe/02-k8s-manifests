apiVersion: v1
kind: ConfigMap
metadata:
  name: harbor-registry-config
  namespace: devops
data:
  config.yml: |
    version: 0.1
    log:
      level: info
    http:
      addr: :5000
      relativeurls: true
      drain_timeout: 60s
      secret: "HARBOR_REGISTRY_SECRET_123456"
      debug:
        addr: :5001
    auth:
      token:
        issuer: harbor-token-issuer
        realm: https://cntlp-onp-wk-01.tail270b85.ts.net/service/token
        service: harbor-registry
        # rootcertbundle: /etc/registry/root.crt  <-- 마운트된 파일이 없으므로 주석 처리
    storage:
      cache:
        layerinfo: redis
      filesystem:
        rootdirectory: /storage
      maintenance:
        uploadpurging:
          enabled: true
          age: 168h
          interval: 24h
          dryrun: false
    redis:
      addr: harbor-redis:6379
      db: 1
