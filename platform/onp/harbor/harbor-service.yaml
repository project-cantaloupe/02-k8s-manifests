apiVersion: v1
kind: Service
metadata:
  name: harbor
  namespace: devops
spec:
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 31432
  selector:
    app: harbor-portal
