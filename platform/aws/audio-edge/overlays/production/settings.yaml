# 실제 배포 전 publicHost 를 팀 소유 도메인으로 교체한다.
#
# acmeEmail 과 route53ZoneID 는 ClusterIssuer 와 함께
# platform/aws/secops/cert-manager/settings.yaml 로 옮겼다.
apiVersion: v1
kind: ConfigMap
metadata:
  name: audio-edge-settings
  namespace: apps
data:
  publicHost: audio.example.com
