apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - deployment-events.yaml
  - service.yaml

# 03-app-audio의 GitHub Actions가 Harbor에 이미지를 푸시한 뒤 커밋 SHA 태그로
# 갱신한다. audio-api와 audio-events가 같은 Image를 쓰므로 항목은 하나다.
images:
  - name: audio-api
    newName: cntlp-onp-wk-01.tail270b85.ts.net/library/app-audio-api
    newTag: 420af0590ae240bb3ea9e3638f862d655ca0dec2
