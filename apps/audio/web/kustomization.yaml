apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

# 03-app-audio의 GitHub Actions가 Harbor에 이미지를 푸시한 뒤 커밋 SHA 태그로
# 이 항목을 갱신한다. 이미지 이름 audio-web은 그 규약에 맞춘 자리표시자다.
images:
  - name: audio-web
    newName: cntlp-onp-wk-01.tail270b85.ts.net/library/web
    newTag: b7ffcfc3399b742dc7e7f534f064061762d6bf6a
